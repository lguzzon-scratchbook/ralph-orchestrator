# Feature Development Preset (Minimal)
#
# Same as feature.yml but using auto-derived instructions.
# Just define triggers/publishes - behaviors are injected automatically.
#
# Usage:
#   ralph run --config presets/feature-minimal.yml --prompt "Add user authentication"

event_loop:
  prompt_file: "PROMPT.md"
  completion_promise: "LOOP_COMPLETE"
  max_iterations: 100
  max_runtime_seconds: 14400
  checkpoint_interval: 5

cli:
  backend: "claude"

core:
  specs_dir: "./specs/"

hats:
  builder:
    name: "Builder"
    description: "Implements code changes. One task, one commit."
    triggers: ["build.task"]
    publishes: ["build.done", "build.blocked"]
    default_publishes: "build.done"
    # No instructions - derived from pub/sub contract!

  reviewer:
    name: "Reviewer"
    description: "Reviews implementation for quality. Does NOT modify code."
    triggers: ["review.request"]
    publishes: ["review.approved", "review.changes_requested"]
    default_publishes: "review.approved"
    # No instructions - derived from pub/sub contract!
